# Commerce checkout engine

A small dependency-free JavaScript checkout service used for the Fixloop demo.
It models catalog lookup, cart normalization, inventory, proportional coupons,
tax, split fulfillment, shipping quotes, promotion stacking, and warehouse
allocation.

```bash
npm test
```
